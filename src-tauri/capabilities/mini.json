{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "mini",
  "description": "Tray-click mini summary: reads snapshots, listens for updates, and toggles its own pin/visibility through Rust commands. Rust does all positioning and always-on-top work, so this window gets no window-management permissions of its own.",
  "windows": ["mini"],
  "permissions": ["core:default", "core:event:default"]
}
